#load packages####
library(hierfstat)
citation("hierfstat")

library(adegenet)
citation("adegenet")

library(dplyr)
citation("dplyr")

library(viridis)
citation("viridis")

library(adegenet)
citation("adegenet")

library(reshape2)
citation("reshape2")

library(ggplot2)
citation("ggplot2")

library(ape)
citation("ape")

library(poppr)
citation("poppr")

library(LEA)
citation("LEA")

library(tidyverse)
citation("tidyverse")

library(forcats)
citation("forcats")

library(gridExtra)
citation("gridExtra")

library(gridGraphics)
citation("gridGraphics")

library(grid)
citation("grid")

library(pegas)
citation("pegas")

library(assignPOP)
citation("assignPOP")

library(radiator)
citation("radiator")

library(readr)
citation("readr")

library(ade4)
citation("ade4")

library(dplyr)
citation("dplyr")

library(mapplots)
citation("mapplots")

library(sf)
citation("sf")

library(rnaturalearth)
citation("rnaturalearth")


